DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Necessário apenas em produção (Vercel Blob), para upload de arquivos.
# Deixe em branco em dev local: o sistema usa public/uploads como fallback.
BLOB_READ_WRITE_TOKEN=""
